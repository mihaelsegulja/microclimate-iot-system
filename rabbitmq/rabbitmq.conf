# Route MQTT client traffic through the application's topic exchange so it
# interoperates with the AMQP 0.9.1 backend (telemetry in / commands out).
# RabbitMQ translates MQTT topic '/' to AMQP routing key '.' and '+' to '*'.
mqtt.exchange = microclimate.events
